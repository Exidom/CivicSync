<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CivicSync User Profile</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>

  <!-- NAVBAR -->
  <header class="navbar">
    <div class="nav-left">
      <img src="logo.png" alt="Logo" class="nav-logo" onclick="window.location.href='/dashboardLoggedIn'">
    </div>

    <nav>
      <a href="dashboardLoggedIn">Home</a>
      <a href="signUpEvents">Volunteer Now!</a>
      <a id="createEvent">Need Volunteers?</a>
    </nav>

    <!-- Profile dropdown -->
    <div class="profile-wrapper">
      <button class="profile-trigger">
        <div class="avatar" id="avatar-initial"></div>
        <span class="profile-email" id="display-email"></span>
        <svg class="chevron" viewBox="0 0 16 16" fill="none">
          <path d="M4 6l4 4 4-4" stroke="currentColor" stroke-width="1.5"
                stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>

      <div class="dropdown">
        <div class="dropdown-header">
          <div class="dh-email" id="header-email"></div>
        </div>

        <div class="section-label">User Information</div>

        <a class="dropdown-item" href="userProfile">
          <div>
            <div class="item-title">User Profile</div>
          </div>
        </a>

        <a class="dropdown-item" id="viewGroups">
          <div>
            <div class="item-title">User Groups</div>
          </div>
        </a>

        <a class="dropdown-item" href="viewOrganization">
          <div>
            <div class="item-title">User Organization</div>
          </div>
        </a>

        <div class="dropdown-divider"></div>

        <a class="dropdown-item sign-out" id="logoutBtn">
          <div class="item-title">Logout</div>
        </a>
      </div>
    </div>
  </header>

  <!-- MAIN CONTENT -->
  <main class="container">
    <div class="tab-vertical-container">
    
      <!-- TABS -->
      <div class="tab-vertical-buttons">
        <button class="tablinks" data-tab="Profile">Edit Profile</button>
        <button class="tablinks" data-tab="Groups">Hours & Groups</button>
        <button class="tablinks" data-tab="Organization">Organization</button>
        <button class="tablinks" data-tab="Events">Events</button>
        <button class="tablinks" data-tab="Medals">Medals</button>
        <button class="tablinks" data-tab="Leaderboard">Leaderboards</button>
      </div>

      <div class="tab-vertical-content">

        <div id="Profile" class="tabcontent">
          <h1>User Profile</h1>

          <form id="profileForm" class="center">
            <label>Email:</label>
            <input type="email" id="email" readonly>

            <label>First Name:</label>
            <input type="text" id="firstName">

            <label>Last Name:</label>
            <input type="text" id="lastName">

            <label>Phone Number (must be 10 digits):</label>
            <input type="tel" id="phone">

            <label>Tell people a little bit about yourself:</label>
            <textarea id="userDescription" rows="3" placeholder="User description"></textarea>

            <button type="submit" class="default-btn">Save Profile</button>
          </form>
        </div>

        <div id="Groups" class="tabcontent">
          <div id="groupSection">
            <h1>Your Hours:</h1>
            <h3 id="totalHours" style="padding-bottom: 30px;"></h3>

            <h1>Your Groups:</h1>
            <div class="event-grid" id="groupList"></div>

            <h1>Join more!</h1>
            <div class="card" style ="border-radius: 8px; border: 1px solid #086375; width: 250px; margin: 0 auto;">
              <form id="groupInviteForm">
                <input type="text" id="groupCode" placeholder="Enter invite code">
                <button type = submit class="default-btn">Join Group</button>
              </form>
            </div>
            <h1 style="margin-top: 50px;">Or, create your own!</h1>
            <div class="card" style ="border-radius: 8px; border: 1px solid #086375; width: 250px; margin: 0 auto;">
              <form id="createGroupForm">
                <input type="text" id="group_name" placeholder="Group Name" required />
                <textarea id="group_intro_text" rows="4" placeholder="Intro Text"></textarea>
                <button type="submit" class="default-btn">Create Group</button>
              </form>
            </div>
          </div>

          <div id="noGroupSection">
            <div class="card" style ="border-radius: 8px; border: 1px solid #086375; width: 250px; margin: 0 auto;">
              <form id="newGroupInviteForm">
                <p>Join a group to gain access to events!</p>
                <input type="text" id="new_group_code" placeholder="Enter invite code">
                <button type= "submit" class="defualt-btn">Join Group</button>
              </form>
            </div>
            <h1>Or, create your own!</h1>
            <div class="card" style ="border-radius: 8px; border: 1px solid #086375; width: 250px; margin: 0 auto;">
              <form id="newCreateGroupForm">
                <input type="text" id="new_group_name" placeholder="Group Name" required />
                <textarea id="new_group_intro_text" rows="4" placeholder="Intro Text"></textarea>
                <button type="submit" class="default-btn">Create Group</button>
              </form>
            </div>
          </div>
        </div>

        <div id="Organization" class="tabcontent">
          <div id="orgSection">
            <h1 style="margin-bottom: 40px;">Your Organization:</h1>
            <div class="card" style="border-radius: 8px; border: 1px solid #086375; min-height: 200px; min-width: 300px;">
              <h3 id="orgName" style="margin-bottom: 100px;"></h3>
              <button class="default-btn" id="viewOrganization">View Organization</button>
              <!-- TODO: Determine if we want a 'delete org' option -->
            </div>
          </div>

          <div id="noOrgSection">
            <h3>You don't have an organization, create one here:</h2>
            <form id="createOrgForm">
              <input type="text" id="orgNameInput" placeholder="Organization name">
              <textarea id="orgIntroInput" rows="4" placeholder="Organization description"></textarea>
              <button type="submit">Create Org</button>
            </form>
          </div>
        </div>

        <div id="Events" class="tabcontent">
          <div id="eventSection">
            <h1>Your Events:</h1>
            <div class="event-grid" id="eventList"></div>
          </div>

          <div id="noEventSection">
            <h1>You have no current events. Sign up here!</h1>
            <button href="signUpEvents" class="view-event-btn">Sign up for events</button>
          </div>
        </div>

        <div id="Medals" class="tabcontent">
          <h1>Your Medals: </h1>
          <div id="medalsList"></div>
        </div>

        <div id="Leaderboard" class="tabcontent">
          <h1>Leaderboards:</h1>
          <div id="leaderboardList" class="event-grid" style="gap: 0px;"></div>
        </div>
      </div>
    </div>
  </main>

    <!-- TODO: Implement the pictures <img id="profile-pic" src="default-profile.png" alt="Profile Picture"> -->
    

  <!-- TODO: Add links to any social media pages we create-->
  <footer class="footer">
    <div>Follow Us: ● ● ● ●</div>
    <div>Contact Us: contactus@civicsync.org</div>
  </footer>

  <script type="module">
    import { handleLogout, fetchWithAuth } from "/js/auth.js"; // your Google auth helper
    import { initUserProfile, initCreateOrg, initJoinGroup, initJoinGroupNEW, submitNewGroup, NEWsubmitNewGroup, loadMedals, loadLeaderboards } from "/js/functions.js";

    document.addEventListener("DOMContentLoaded", async () => {

      const openTab = sessionStorage.getItem("openTab");
      if (openTab) {
        sessionStorage.removeItem("openTab");
        const tabBtn = document.querySelector(`.tab-vertical-buttons button[data-tab="${openTab}"]`);
        if (tabBtn) tabBtn.click();
      }

      await initUserProfile();
      initCreateOrg();
      initJoinGroup();
      initJoinGroupNEW();
      submitNewGroup();
      NEWsubmitNewGroup();
      await loadMedals();
      await loadLeaderboards();

      // Page navigation
      const routes = {
        viewGroup: "/viewGroup",
        createGroups: "/createGroups",
        viewOrganization: "/viewOrganization",
        signUpEvents: "/signUpEvents"
      };
      Object.entries(routes).forEach(([id, path]) => {
        const el = document.getElementById(id);
        if (el) {
          el.addEventListener("click", () => {
            window.location.href = path;
          });
        }
      });

      // Group navigation
      const groupList = document.getElementById("groupList");

      if (groupList) {
        groupList.addEventListener("click", (e) => {
          if (e.target.classList.contains("view-group-btn")) {
            const gid = e.target.dataset.gid;

            // store selected group
            localStorage.setItem("groupViewId", gid);

            // Remember current tab
            sessionStorage.setItem("openTab", "Groups");

            window.location.href = "/group";
          }
        });
      }

      // Event navigation
      const eventList = document.getElementById("eventList");

      if (eventList) {
        eventList.addEventListener("click", (e) => {
          if (e.target.classList.contains("view-event-btn")) {
            const sid = e.target.dataset.sid;

            sessionStorage.setItem("selectedSid", sid);
            sessionStorage.setItem("openTab", "Events"); 

            window.location.href = "/eventDetails";
          }
        });
      }

      // Navbar user info
      const user = await fetchWithAuth("/api/userProfileData");

      if (user && user.display_name) {
        const email = user.display_name;

        document.getElementById("display-email").textContent = email;
        document.getElementById("header-email").textContent = email;
        document.getElementById("avatar-initial").textContent = email[0].toUpperCase();
      }

      document.getElementById("logoutBtn").onclick = async () => {
        await handleLogout();
        window.location.href = "/dashboard";
      };

      if (!user.hasOrg) {
        document.getElementById("createEvent").onclick = async () => {
        window.location.href = "/viewOrganization";
        }
      }
      else {
        document.getElementById("createEvent").onclick = async () => {
          sessionStorage.setItem("openTab", "CreateEvent");
          window.location.href = "/viewOrganization";
        }
      }

      document.getElementById("viewGroups").onclick = async () => {
        sessionStorage.setItem("openTab", "Groups");
        window.location.href = "/userProfile";
      }

    });

    //   if (user.photoURL) {
    //     profilePic.src = user.photoURL;
    //   }

    // // Preview selected profile picture
    // photoInput.addEventListener('change', (e) => {
    //   const file = e.target.files[0];
    //   if (file) {
    //     profilePic.src = URL.createObjectURL(file);
    //   }
    // });

    //   const profileData = {
    //     photoFile: photoInput.files[0] || null
    //   };

    //   console.log('Profile data to save:', profileData);

    // document.getElementById("userName").innerText = user.name;
    // document.getElementById("userPic").src = user.picture;
    // TODO: Need to get user info to appear by profile picture!

  </script>
</body>
</html>
